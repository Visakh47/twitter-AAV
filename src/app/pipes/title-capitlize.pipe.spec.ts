import { TitleCapitlizePipe } from './title-capitlize.pipe';

describe('TitleCapitlizePipe', () => {
  it('create an instance', () => {
    const pipe = new TitleCapitlizePipe();
    expect(pipe).toBeTruthy();
  });
});
